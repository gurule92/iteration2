<%= form_with(model: tier_item) do |form| %>
  <% if tier_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tier_item.errors.count, "error") %> prohibited this tier_item from being saved:</h2>

      <ul>
        <% tier_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :tier %>
    <%= form.number_field :tier %>
  </div>

  <div class="field">
    <%= form.label :justification %>
    <%= form.text_area :justification %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
